{
  "dependencies": {
    "@solana/wallet-adapter-backpack": "^0.1.14",
    "@solana/wallet-adapter-react": "^0.15.39",
    "@solana/wallet-adapter-react-ui": "^0.9.39",
    "@solana/wallet-adapter-solflare": "^0.6.32"
  },
  "scripts": {
    "anchor:build": "anchor build && npm run copy:idl",
    "copy:idl": "cp target/idl/defighter.json web/idl/defighter.json && cp target/types/defighter.ts web/target_types_proxy.d.ts",
    "anchor:deploy:devnet": "solana config set --url https://api.devnet.solana.com && anchor deploy --provider.cluster devnet && npm run copy:idl",
    "anchor:deploy:localnet": "solana config set --url http://127.0.0.1:8899 && anchor deploy --provider.cluster localnet && npm run copy:idl"
  }
}
